# 实验5：进程管理与调度

## 一、 实验概述

### 实验目标
深入理解操作系统的进程模型与调度机制。本实验旨在实现进程控制块（PCB）、上下文切换（Context Switch）、基于时间片的抢占式轮转调度器（Round-Robin Scheduler），以及基础的进程同步机制（Sleep/Wakeup）。

### 完成情况
- **进程抽象**：定义了 `struct proc` 和 `struct context`，实现了进程状态管理。
- **上下文切换**：编写了汇编代码 `swtch.S` 实现寄存器换入换出。
- **调度器**：实现了 `scheduler` 核心循环和 `sched/yield` 机制。
- **抢占式调度**：在时钟中断中实现了强制让出 CPU 的逻辑，成功验证了长任务被打断。
- **同步机制**：实现了自旋锁（Spinlock）保护和生产者-消费者模型。

---

## 二、 技术设计

### 1. 系统架构设计

我的进程管理系统主要由以下模块组成，运行流程如下：

```
[时钟中断] -> [kerneltrap] -> [yield] -> [sched]
                                          ↓
[allocproc] -> [进程A] --(swtch)--> [调度器(Scheduler)] --(swtch)--> [进程B]
                                          ↑
                                   [寻找RUNNABLE进程]
```

**与 xv6 的异同**：
- **相同点**：采用了经典的 `context` 保存被调用者保存寄存器（Callee-saved registers），使用了 `proc` 数组和状态机（RUNNING, RUNNABLE, SLEEPING 等）。
- **不同点**：
    - **简化启动**：为了测试方便，直接在内核态创建了 `test_runner` 线程，而非从磁盘加载 `initcode`。
    - **手动中断管理**：在 `main.c` 中显式配置了 S 模式的中断分闸（`sie`），以适应裸机环境下的中断开启需求。

### 2. 关键数据结构

```c
// 进程控制块 (kernel/proc.h)
struct proc {
    struct spinlock lock;        // 保护进程状态的自旋锁
    enum procstate state;        // 进程状态 (UNUSED, RUNNING, RUNNABLE...)
    uint64 kstack;               // 内核栈虚拟地址
    struct context context;      // 进程切换上下文 (ra, sp, s0-s11)
    void *chan;                  // 休眠等待通道
    int pid;                     // 进程ID
    // ...
};

// 上下文结构 (仅保存被调用者保存寄存器)
struct context {
    uint64 ra; // 返回地址
    uint64 sp; // 栈指针
    uint64 s0; // 帧指针
    uint64 s1 - s11; // 其他通用寄存器
};
```

**设计理由**：根据 RISC-V 调用约定（Calling Convention），`a0-a7` 等寄存器由调用者保存，因此在 `swtch` 函数中只需保存 `ra`, `sp` 和 `s0-s11`，大大减少了切换开销。

### 3. 核心算法：抢占式轮转调度

1.  **调度循环**：`scheduler` 函数在一个死循环中遍历 `proc` 数组。
2.  **锁机制**：找到 `RUNNABLE` 进程后，持有该进程的锁（保护状态转换），将状态改为 `RUNNING`。
3.  **切换**：调用 `swtch` 切换到目标进程上下文。
4.  **抢占**：在 `trap.c` 中，若检测到时钟中断且当前有进程运行，调用 `yield()`。`yield` 会保存状态并调回 `scheduler`。

---

## 三、 实现细节

### 1. 上下文切换汇编 (`swtch.S`)

这是实现多任务的基石。

```asm
.globl swtch
swtch:
    # a0 = old_context, a1 = new_context
    # 1. 保存旧进程的寄存器到 old_context
    sd ra, 0(a0)
    sd sp, 8(a0)
    sd s0, 16(a0)
    # ... (保存 s1-s11)
    
    # 2. 从 new_context 恢复新进程的寄存器
    ld ra, 0(a1)
    ld sp, 8(a1)
    # ... (恢复 s1-s11)
    
    ret # 返回到新进程的 ra 地址 (通常是 forkret 或 scheduler)
```

### 2. 抢占逻辑 (`kernel/trap.c`)

为了防止某个死循环进程独占 CPU，必须实现抢占。

```c
void kerneltrap() {
    // ... 保存 sepc, sstatus ...
    
    // 判断是否是时钟中断 (devintr 返回 2)
    if(which_dev == 2 && myproc() != 0 && myproc()->state == RUNNING) {
        yield(); // 强制让出 CPU
    }
    
    // ... 恢复 sepc, sstatus ...
}
```

### 3. 难点突破：中断使能的“鬼打墙”

**问题**：在实现调度器时，发现进程切换后中断始终处于关闭状态，导致时钟中断无法进入，进程无法被抢占，最终卡死在第一个任务中。

**根本原因**：
1.  **宏定义错误**：`riscv.h` 中 `SSTATUS_SIE` 的位定义不正确。
2.  **模式混淆**：在 `start.c` (M-mode) 设置了 `mie`，但切换到 `main.c` (S-mode) 后，S-mode 的中断分闸 `sie` 寄存器默认是关闭的。

**解决方案**：
在 `main.c` 入口处显式开启 S 模式的所有中断使能位：
```c
// kernel/main.c
void main(void) {
    // 关键修复：确保 S 模式接收时钟、外部和软件中断
    w_sie(r_sie() | SIE_SEIE | SIE_STIE | SIE_SSIE); 
    // ...
}
```

---

## 四、 测试与验证

### 1. 功能测试

我编写了一个综合测试函数 `test_runner`，包含以下测试项：

- **进程创建**：验证 `allocproc` 是否能分配不同的 PID。
- **轮转调度**：创建多个简单任务，验证是否交替输出。
- **同步机制**：生产者-消费者模型，验证 `sleep/wakeup` 和锁的正确性。
- **抢占测试**：创建一个执行 1亿次空循环的长任务，验证其是否会被时钟中断打断。

### 2. 运行结果

以下是 QEMU 的实际运行日志截图（文本形式）：

```text
Userinit: Created test_runner process (PID 1)
=== Starting Experiment 5 Tests ===
[Test] Process Creation...
SUCCESS: Created processes PID 2 and 3

=== Process Table ===
PID: 1 | State: RUNNING
PID: 2 | State: RUNNABLE
PID: 3 | State: RUNNABLE
=====================

... (System starts scheduling) ...

Simple task running (PID 2)
Simple task running (PID 3)
Producer: Produced 10
Task PID 6 running iteration 0
Task PID 6 running iteration 1
Task PID 6 running iteration 2
Task PID 6 running iteration 3  <-- PID 6 正在运行
Task PID 7 running iteration 0  <-- PID 6 被打断！PID 7 插队！
...
Task PID 8 running iteration 3
Consumer: Consumed 10           <-- 消费者被唤醒并运行
Producer: Produced 20
Task PID 6 finished.            <-- PID 6 终于回来执行完毕
```

**验证结论**：
1.  **抢占成功**：PID 6 在 `iteration 3` 后被 PID 7 打断，并没有连续执行到 `finished`，证明时钟中断成功触发了 `yield`。
2.  **同步成功**：消费者严格在生产者 `Produced 10` 之后才输出 `Consumed 10`，证明休眠唤醒逻辑正确。

---

## 五、 问题与总结

### 1. 遇到的问题与解决

#### 问题 1：调度器启动后 Panic "acquire"
**现象**：系统启动 `test_runner` 后立即 Panic。
**原因**：`allocproc` 函数在分配完进程后，忘记释放进程锁 (`release`)。导致 `userinit` 带着锁返回，当调度器试图再次运行该进程时，尝试获取已被持有的锁，触发 Panic。
**解决**：在 `allocproc` 返回前添加 `release(&p->lock)`。

#### 问题 2：进程卡死，无抢占现象
**现象**：第一个进程运行后，系统不再输出，也不切换到其他进程。调试发现 `intr_get()` 始终返回 0。
**原因**：RISC-V 的中断开启机制（`sstatus.SIE`）依赖正确的宏定义，且 S 模式下的 `sie` 寄存器需要在 S 模式代码中显式初始化。M 模式下的设置不会自动完全继承给 S 模式的分闸。
**解决**：修正 `riscv.h` 宏定义，并在 `kernel/main.c` 中添加 `w_sie(...)` 初始化代码。

### 2. 实验收获

1.  **深刻理解了“上下文”**：上下文不仅仅是寄存器，还包括锁的状态（`noff`, `intena`）。任何一个状态恢复错误，都会导致系统行为异常。
2.  **调试能力的提升**：学会了通过在 `scheduler` 和 `task` 中打印寄存器状态（如 `sstatus`）来定位中断丢失的根本原因。
3.  **对抢占的直观认识**：通过观察日志中 PID 输出的“乱序”，直观地感受到了操作系统是如何通过时间片强行剥夺进程 CPU 使用权的。

### 3. 改进方向
当前 `allocproc` 使用简单的线性遍历查找空闲槽位，时间复杂度为 O(N)。如果 `NPROC` 很大，效率较低。未来可以使用空闲链表或位图来优化进程分配算法。