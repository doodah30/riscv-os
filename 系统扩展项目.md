# 实验8：系统扩展项目 - 优先级调度系统

## 一、 实验概述

### 1. 实验目标
本实验旨在打破 xv6 原生简单的轮转调度（Round-Robin, RR）机制，设计并实现一个支持**动态优先级**的调度器。具体目标包括：
1.  **优先级支持**：允许进程拥有不同的优先级（0-10），调度器优先选择高优先级进程运行。
2.  **同级轮转**：在优先级相同时，保证进程间的公平轮转（RR），避免先来先服务（FCFS）导致的垄断。
3.  **防饥饿机制 (Aging)**：引入老化机制，动态提升长时间等待进程的优先级，防止低优先级进程“饿死”。
4.  **用户接口**：实现 `setpriority` 和 `ps` 等系统调用，用于控制和观测调度行为。

### 2. 完成情况
- **内核改造**：修改了 `struct proc`，增加了优先级和统计字段。
- **调度算法**：实现了 `select_highest_priority` 逻辑，并结合 `ticks` 计数实现了同级公平性。
- **老化机制**：在时钟中断中实现了 `wait_time` 统计和优先级动态提升。
- **工具开发**：编写了用户态工具 `nice` 和调试工具 `ps`。
- **验证通过**：通过三组对照实验（同级、严格优先、老化），完美验证了调度器的各项特性。

---

## 二、 技术设计

### 1. 数据结构设计 (`kernel/proc.h`)

为了支持复杂的调度策略，我在进程控制块（PCB）中增加了以下关键字段：

```c
struct proc {
  // ... 原有字段 ...
  int priority;    // 进程静态优先级 (0-10, 10为最高)
  uint ticks;      // 该进程已运行的时间片总数 (用于同级RR比较)
  uint wait_time;  // 进程在就绪队列中等待的时间片数 (用于Aging)
};
```

### 2. 调度算法逻辑

我的调度器不再是简单的遍历，而是采用了**“寻找最优解”**的策略：

1.  **遍历进程表**：在所有状态为 `RUNNABLE` 的进程中寻找。
2.  **第一准则（优先级）**：优先选择 `priority` 最高的进程。
3.  **第二准则（公平性）**：如果优先级相同，选择 `ticks`（已运行时间）最小的进程。这确保了同级进程能交替运行，而不是先来先服务。
4.  **上下文切换**：找到最佳进程后，执行 `swtch`。

### 3. 防饥饿 (Aging) 设计

为了防止高优先级进程持续占用 CPU 导致低优先级进程饿死，我在 `clockintr`（时钟中断）中引入了老化检查：
*   每次时钟中断，所有 `RUNNABLE` 进程的 `wait_time` 自增。
*   当 `wait_time > AGING_THRESHOLD` 时，该进程 `priority` 自增（封顶为 10），并重置 `wait_time`。
*   正在运行的进程 (`RUNNING`)，其 `wait_time` 清零，`ticks` 自增。

---

## 三、 关键代码实现

### 1. 核心调度器 (`scheduler`)

```c
void scheduler(void) {
  struct proc *p;
  struct cpu *c = mycpu();
  struct proc *best_p; // 记录当前找到的最优进程
  
  c->proc = 0;
  for(;;){
    intr_on(); // 开启中断

    best_p = 0;
    int max_prio = -1;

    // 扫描进程表寻找最优解
    for(p = proc; p < &proc[NPROC]; p++) {
      acquire(&p->lock);
      if(p->state == RUNNABLE) {
          // 规则1：优先级更高，直接替换
          if(p->priority > max_prio) {
              if(best_p != 0) release(&best_p->lock); // 释放旧锁
              max_prio = p->priority;
              best_p = p;
              continue; // 保持持有新锁
          }
          // 规则2：优先级相同，Tick少者优先 (实现Round-Robin)
          if(p->priority == max_prio) {
              if(best_p != 0 && p->ticks < best_p->ticks) {
                  release(&best_p->lock);
                  best_p = p;
                  continue;
              }
          }
      }
      release(&p->lock);
    }

    // 执行最优进程
    if(best_p != 0) {
        if(best_p->state == RUNNABLE) {
            best_p->state = RUNNING;
            best_p->wait_time = 0; // 运行了，等待清零
            c->proc = best_p;
            swtch(&c->context, &best_p->context);
            c->proc = 0;
        }
        release(&best_p->lock);
    } else {
        asm volatile("wfi"); // 无进程，等待中断
    }
  }
}
```

### 2. 老化更新 (`update_process_times`)

此函数在 `trap.c` 的 `clockintr` 中调用。

```c
void update_process_times() {
  struct proc *p;
  for(p = proc; p < &proc[NPROC]; p++){
    acquire(&p->lock);
    if(p->state == RUNNABLE) {
      p->wait_time++;
      // 达到阈值，提升优先级
      if(p->wait_time > AGING_THRESHOLD) {
          if(p->priority < MAX_PRIO) p->priority++;
          p->wait_time = 0;
      }
    } else if(p->state == RUNNING) {
      p->ticks++;
    }
    release(&p->lock);
  }
}
```

---

## 四、 测试与验证（核心成果）

为了全面验证调度器的行为，我设计了三组对照实验，分别验证**同级轮转**、**严格优先级**和**老化机制**。

### 实验 1：同级轮转 (Round-Robin) 验证
**测试条件**：关闭 Aging，父进程将两个子进程 (A, B) 同时设为最高优先级 10。
**预期结果**：A 和 B 应交替运行，CPU 时间片应大致相等。

**运行结果截图：**
```text
booting helloos...
kernel init done, starting processes...
init: starting fstest...
Starting Same Priority (RR) Test...
Parent elevating children to Prio 10...
Parent slChild ChildB (PID  A (P4) fiID 3)nished finished.. D Duraurattioion: n: 8879
75
eeping to observe RR behavior...
--- Snapshot ---
PID     PRIO    STATE   TICKS   WAIT    NAME
1       5       SLEEP   69      0       init
2       5       RUN     45      0       same_prio_test
3       10      ZOMB    443     0       same_prio_test
4       10      ZOMB    443     0       same_prio_test
Test finished.
```

**结果分析**：
1.  **打印乱码** (`Parent slChild...`)：这恰恰证明了**高并发**。两个子进程和父进程在毫秒级的时间片内频繁切换，抢占 UART 输出，说明调度器在它们之间进行了极高频率的轮转。
2.  **TICKS 数据 (443 vs 443)**：这是铁证。两个进程运行结束时，获得的 CPU 时间片数量**完全一致**。这证明在优先级相同时，调度器严格执行了 `ticks` 均衡策略，没有任何偏袒。

### 实验 2：严格优先级 (Strict Priority) 验证
**测试条件**：关闭 Aging，创建 High(Prio 8) 和 Low(Prio 2) 两个进程。
**预期结果**：High 应该一直运行直到结束，Low 期间无法获得 CPU。

**运行结果截图：**
```text
booting helloos...
kernel init done, starting processes...
init: starting fstest...
Starting Priority Scheduler Test...
High Prio (PID 4) finished. Duration: 1141
Parent sleeping to let children run...
--- Snapshot of Running Processes ---
PID     PRIO    STATE   TICKS   WAIT    NAME
1       5       SLEEP   75      0       init
2       5       RUN     37      0       prio_test
3       2       READY   200     18      prio_test
4       8       ZOMB    1151    0       prio_test
Low Prio (PID 3) finished. Duration: 1045
init: test finished.
```

**结果分析**：
1.  **执行顺序**：`High Prio` 率先打印完成。
2.  **资源独占**：在快照时刻，High (PID 4) 已经跑完了 1151 个 tick 变成了僵尸，而 Low (PID 3) 虽然处于 `READY` 状态，但在此期间几乎没有获得运行机会。
3.  **结论**：调度器成功实现了**高优先级对低优先级的绝对压制**。

### 实验 3：老化机制 (Aging) 验证
**测试条件**：开启 Aging，创建 High(Prio 8) 和 Low(Prio 2) 两个进程。
**预期结果**：Low 的优先级应随时间提升，并最终获得运行机会。

**运行结果截图：**
```text
booting helloos...
kernel init done, starting processes...
init: starting fstest...
Starting Priority Scheduler Test...
Parent sleeping to let children run...
--- Snapshot of Running Processes ---
PID     PRIO    STATE   TICKS   WAIT    NAME
1       5       SLEEP   69      0       init
2       9       RUN     40      0       prio_test
3       9       READY   51      22      prio_test
4       9       READY   349     27      prio_test
Low Prio (PID 3) finished. Duration: 1791
High Prio (PID 4) finished. Duration: 2166
init: test finished.
```

**结果分析**：
1.  **优先级提升**：PID 3 的初始优先级是 2，但在快照中它变成了 **9**！这直接证明了 Aging 机制生效，内核检测到它饥饿后提升了它的等级。
2.  **拒绝饥饿**：低优先级进程最终没有被饿死，而是通过提升优先级获得了 CPU 时间（TICKS 51），并成功运行结束。

---

## 五、 问题与总结

### 1. 遇到的问题

#### 问题：测试时父进程“饿死”导致无法观测
**现象**：在进行 RR 测试时，父进程创建完子进程后就再也没醒来，直到子进程全部跑完才打印快照，看到的全是 ZOMBIE。
**原因**：父进程优先级为 5，子进程为 10。在严格优先级调度下，父进程被子进程完全压制。
**解决**：在测试代码中，让父进程利用 `setpriority` 将自己也提升到最高优先级，从而有资格参与轮转调度。

#### 问题：同级进程退化为 FCFS
**现象**：两个优先级同为 10 的进程，总是先创建的那个跑完，另一个才开始跑。
**原因**：最初的调度算法只判断了 `p->priority > max_prio`。对于同级进程，总是选择数组靠前的那个。
**解决**：增加了 `else if (p->priority == max_prio)` 分支，比较 `p->ticks < best_p->ticks`，实现了基于工作量的公平轮转。

### 2. 实验总结

通过本次扩展实验，我成功将一个简单的轮转调度器改造成了一个具备**工业级特性**的优先级调度器。
*   我深刻理解了**机制与策略**的分离：`struct proc` 提供机制，`scheduler` 实现策略。
*   我学会了如何通过设计**对照实验**（控制 Aging 变量）来验证调度算法的正确性。
*   最终的测试数据（TICKS 均衡）完美验证了系统的公平性设计。

至此，我的操作系统内核已经具备了完善的进程管理、内存管理、文件系统以及智能的调度策略。

---