# 实验6：系统调用

## 一、 实验概述

### 1. 实验目标
本实验旨在实现用户态（User Mode）与内核态（Kernel Mode）之间的切换机制。通过构建陷阱帧（Trapframe）、跳板页（Trampoline）和系统调用分发器，使内核能够运行用户进程，并响应 `write`, `exit`, `fork`, `wait` 等核心系统调用。

### 2. 完成情况
- **内存布局**：实现了用户虚拟地址空间布局，包括 `TRAMPOLINE` 和 `TRAPFRAME` 的特殊映射。
- **陷阱处理**：编写了 `trampoline.S` 汇编代码，实现了 `uservec` 和 `userret` 用于保存/恢复用户上下文。
- **系统调用**：实现了 `syscall` 分发器，以及 `sys_write`, `sys_exit`, `sys_fork`, `sys_wait` 等功能。
- **安全性增强**：在 `copyin`/`copyout` 中增加了对用户指针的合法性检查（PTE_U 权限检查）。
- **综合测试**：编写了 `systest.c` 测试程序，通过了基础功能、参数传递、内存安全及性能测试。

### 3. 开发环境
- **OS**: Ubuntu 22.04 LTS
- **QEMU**: 7.2.0
- **Toolchain**: riscv64-unknown-elf-gcc

---

## 二、 技术设计

### 1. 系统架构：Trap 机制流转

本实验的核心是打通用户态到内核态的“传送门”。流程如下：

```
[用户态: systest] 
      ↓ ecall 指令
[Trampoline页: uservec] (保存用户寄存器到 trapframe)
      ↓ 切换页表 (User -> Kernel)
[内核态: usertrap]
      ↓
[syscall 分发器] -> [sys_fork / sys_write / ...]
      ↓
[内核态: usertrapret] (填充 trapframe，准备返回)
      ↓ 
[Trampoline页: userret] (恢复用户寄存器，切换页表 Kernel -> User)
      ↓ sret 指令
[用户态: 继续执行]
```

### 2. 关键数据结构

**`struct trapframe` (kernel/proc.h)**
为了在用户态和内核态之间传递数据，定义了陷阱帧结构体。它必须被映射到用户空间的固定高地址 `TRAPFRAME`，这样用户代码在不切换页表的情况下也能保存寄存器。

### 3. 核心设计决策：Trampoline 页

**问题**：当发生中断/异常时，`stvec` 指向的地址必须是当前页表能访问的。但用户页表没有内核代码的映射。
**解决**：
1.  在物理内存中分配一页存放 `trampoline.S` 的代码。
2.  在**内核页表**和**所有用户页表**中，都将这一页映射到同一个最高的虚拟地址 `TRAMPOLINE` (MAXVA - PGSIZE)。
3.  这样，无论当前使用哪个页表，CPU 都能在同一个虚拟地址找到 Trap 处理入口。

### 4. 安全性设计
为了防止用户程序破坏内核，我在 `vm.c` 的 `copyin/copyout` 函数中使用了 `walkaddr` 进行地址转换。`walkaddr` 会强制检查页表项的 **PTE_U** 位，如果用户试图传入内核空间的地址（无 PTE_U 权限），系统调用将直接返回 -1，而不是触发内核 Panic。

---

## 三、 实现细节与关键代码

### 1. 用户态返回机制 (`usertrapret`)

这是内核把控制权交还给用户的关键函数。

```c
void usertrapret(void) {
  struct proc *p = myproc();
  intr_off(); // 关中断，保证原子性

  // 1. 设置 stvec 指向 trampoline 中的 uservec
  // 必须使用 TRAMPOLINE 虚拟地址，而不是物理地址
  uint64 trampoline_uservec = TRAMPOLINE + (uint64)uservec - (uint64)trampoline;
  w_stvec(trampoline_uservec);

  // 2. 填充 Trapframe，供下一次 Trap 使用
  p->trapframe->kernel_satp = r_satp();         // 内核页表
  p->trapframe->kernel_sp = p->kstack + PGSIZE; // 内核栈
  p->trapframe->kernel_trap = (uint64)usertrap;

  // 3. 配置 SSTATUS，开启用户态中断 (SPIE=1, SPP=0)
  unsigned long x = r_sstatus();
  x &= ~SSTATUS_SPP; 
  x |= SSTATUS_SPIE; 
  w_sstatus(x);

  // 4. 准备跳转到 userret
  uint64 fn = TRAMPOLINE + (uint64)userret - (uint64)trampoline;
  ((void (*)(uint64,uint64))fn)(TRAPFRAME, MAKE_SATP(p->pagetable));
}
```

### 2. 参数安全检查 (`syscall.c`)

```c
int argaddr(int n, uint64 *ip) {
  int x;
  struct proc *p = myproc();
  if(argint(n, &x) < 0) return -1;
  
  // 基础边界检查：地址不能超出进程大小
  if((uint64)x >= p->sz || (uint64)x + 8 > p->sz)
    return -1;
  *ip = x;
  return 0;
}
```

### 3. 内存映射 (`vm.c`)

为了支持用户进程，在 `kvminit` (内核映射) 和 `uvmcreate` (用户映射) 中都强制映射了 Trampoline。

```c
// kernel/vm.c
// 无论内核还是用户，这一行映射至关重要
if(mappages(pagetable, TRAMPOLINE, PGSIZE, (uint64)trampoline, PTE_R | PTE_X) < 0){
    panic("mappages trampoline");
}
```

---

## 四、 测试与验证

### 1. 测试方法
利用实验7实现的文件系统和 `exec` 机制，编写了用户态测试程序 **`user/systest.c`**。该程序包含四个子测试模块，全面覆盖实验6的要求。

### 2. 运行结果截图

**QEMU 实际运行日志：**

```text
=== Starting Lab 6 System Call Tests ===
[TEST] Basic Syscalls (getpid, fork, wait, exit)...
  Current PID: 2
  Child exiting with magic status 88...
  Parent received correct status: 88
PASSED
[TEST] Parameter Passing (write)...
Hello
PASSED
[TEST] Security (Invalid Pointers)...
  Write to kernel addr handled correctly (ret=-1)
  Read to kernel addr handled correctly (ret=-1)
PASSED
[TEST] System Call Performance...
  100000 getpid() calls took 1246 ms
  Average: 12.4 us per call
PASSED
=== All Lab 6 Tests Passed! ===
```

### 3. 结果分析

#### (1) 基础功能测试
*   **现象**：父进程创建子进程，子进程调用 `exit(88)`，父进程通过 `wait` 成功获取到状态码 88。
*   **结论**：证明 `fork` 正确复制了进程状态，`exit` 正确传递了退出码，`wait` 正确回收了僵尸进程。进程间通信（IPC）机制工作正常。

#### (2) 参数传递测试
*   **现象**：`write(1, "Hello", 5)` 成功在屏幕打印 "Hello"。
*   **结论**：内核正确解析了用户态传入的文件描述符、缓冲区指针和长度参数。

#### (3) 安全性测试 (Security)
*   **现象**：当测试程序试图调用 `write(1, (void*)0x80000000, 1)`（向内核物理地址写入）时，内核**没有崩溃**，而是返回了错误码 `-1`。
*   **结论**：内存保护机制生效。`copyin` 函数中的 `walkaddr` 成功拦截了非法指针，保护了内核空间不被用户程序破坏。

#### (4) 性能测试 (Performance)
*   **数据**：100,000 次 `getpid` 调用耗时 1246ms，平均每次 **12.4us**。
*   **结论**：在 QEMU 模拟环境下，12.4us 的系统调用开销（包含用户态/内核态切换、寄存器保存恢复、C函数调用）处于非常优秀的范围内，证明 Trap 处理路径没有明显的性能瓶颈。

---

## 五、 问题与总结

### 1. 遇到的问题与解决

#### 问题 1：启动后卡死，无任何输出
**现象**：开启分页 (`kvminithart`) 后系统直接挂起。
**原因**：链接脚本 (`kernel.ld`) 未正确处理 `trampoline` 段，导致其物理地址被视为 0。
**解决**：
1.  修改 `kernel.ld`，使用 `ALIGN(0x1000)` 对齐 `trampoline` 段。
2.  修正 `kvminit` 中的 `kvmmap` 参数顺序。

#### 问题 2：Trap 切换时崩溃 (Instruction Page Fault)
**现象**：`forkret` 之后，尝试跳转到用户态时系统崩溃。
**原因**：只在内核页表映射了 `TRAMPOLINE`，但忘记在**用户页表** (`uvmcreate`) 中也进行同样的映射。当 `satp` 切换到用户页表后，CPU 无法访问位于高地址的 `userret` 代码。
**解决**：在 `uvmcreate` 函数中显式添加 `mappages(..., TRAMPOLINE, ...)`。

#### 问题 3：系统调用号不匹配
**现象**：`initcode` 运行时报错 `unknown sys call 93`。
**原因**：最初使用的机器码是基于 Linux 标准（Exit=93），而内核实现基于 xv6 标准（Exit=2）。
**解决**：统一使用 xv6 的系统调用编号，并重新生成机器码/汇编库。

### 2. 实验收获

本实验是操作系统中最具挑战性的部分之一。通过亲手实现 `trampoline` 和 `trap` 流程，我深刻理解了：
1.  **虚拟内存的隔离作用**：Trampoline 页是连接两个世界的唯一桥梁。
2.  **特权级切换的细节**：`sret` 指令不仅仅是跳转，还涉及 `SPP`、`SPIE` 等状态位的原子操作。
3.  **调试技巧**：学会了使用 `nm` 查看符号表，以及通过 panic 打桩法定位页表映射错误。

### 3. 更新

本文更新于实现文件管理之后，之前虽然打通了系统调用，但还无法运行真正的 ELF 程序。下一步（实验7）将实现文件系统和 `exec` 系统调用，届时将能够运行标准的 C 语言测试程序。现在就是可以运行的时候了。