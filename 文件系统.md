# 实验报告：文件系统

## 一、 实验概述

### 1. 实验目标
本实验旨在为操作系统构建一个持久化的存储系统和用户程序执行环境。具体目标包括：
1.  **底层驱动**：实现 VirtIO 磁盘驱动，打通内核与底层块设备的通信。
2.  **文件系统核心**：实现缓冲区缓存（Buffer Cache）、日志系统（Logging）、Inode 管理、目录与路径解析。
3.  **文件抽象**：实现文件描述符（File Descriptor）接口，支持 `open`, `read`, `write`, `close` 等系统调用。
4.  **程序加载**：实现 `exec` 系统调用，使其能解析 ELF 格式文件并加载运行磁盘上的用户程序。
5.  **综合测试**：通过编写用户态测试程序 `fstest`，验证文件系统的完整性、并发安全性及性能。

### 2. 完成情况
- **磁盘驱动**：实现了基于 MMIO 的 VirtIO-Blk 驱动，成功识别 QEMU 挂载的磁盘。
- **文件系统栈**：完成了从 Bio 到 Sysfile 的全栈实现，支持元数据管理和 crash-safety（崩溃一致性）。
- **用户进程加载**：实现了 `exec`，成功将手写的 `initcode` 替换为 C 语言编写的 `init` 和 `fstest` 程序。
- **标准输出修复**：重写了用户态 `printf`，支持 64 位参数解析，修复了输出乱码问题。
- **高强度测试**：通过了完整性测试、并发读写测试、持久化测试（模拟断电）以及 1MB 数据写入的性能测试。

### 3. 开发环境
- **OS**: Ubuntu 22.04 LTS
- **QEMU**: 7.2.0 (VirtIO Version 2)
- **Toolchain**: riscv64-unknown-elf-gcc

---

## 二、 技术设计

### 1. 系统架构：七层模型

文件系统采用分层架构设计，自底向上依次为：

1.  **磁盘层 (Disk Layer)**: 通过 `virtio_disk.c` 与硬件交互，读写扇区。
2.  **缓存层 (Buffer Cache)**: `bio.c`，通过 LRU 算法缓存磁盘块，这是性能的关键，并使用 `sleep-lock` 保证对块的互斥访问。
3.  **日志层 (Logging)**: `log.c`，实现简单的预写日志（Write-Ahead Log），保证文件系统操作（如创建文件）的原子性，防止崩溃导致文件系统损坏。
4.  **Inode 层**: `fs.c`，提供无文件名的 Inode 读写操作 (`readi`/`writei`)。
5.  **目录层 (Directory)**: 将文件名映射到 Inode 编号。
6.  **路径层 (Pathname)**: 递归解析路径（如 `/home/user/file`）。
7.  **文件描述符层 (File Descriptor)**: `file.c`，统一了文件、管道、设备（Console）的接口，向用户提供统一视图。

### 2. 核心数据结构

*   **`struct buf` (Buffer)**: 包含磁盘数据拷贝、设备号、块号、引用计数 (`refcnt`) 和睡眠锁。使用了双向链表维护 LRU 队列。
*   **`struct inode` (In-memory Inode)**: 包含文件类型、链接数、大小和数据块地址索引（直接索引+间接索引）。
*   **`struct file`**: 代表一个打开的文件，包含偏移量 (`off`)、读写权限和指向 Inode 的指针。

### 3. 用户程序加载 (`exec`) 流程

`exec(path, argv)` 是将程序从磁盘加载到内存的关键：
1.  **Namei**: 根据路径找到磁盘上的可执行文件 Inode。
2.  **Read Header**: 读取 ELF 文件头，检查魔数（Magic Number）。
3.  **Load**: 遍历 Program Headers，通过 `uvmalloc` 分配物理内存，并使用 `loadseg` 将代码/数据段从磁盘读入内存。
4.  **Stack**: 分配用户栈，并将参数字符串 (`argv`) 拷贝到栈顶。
5.  **Clean Up**: 调用 `proc_freepagetable` 释放旧进程的页表和内存（这是调试的重灾区）。
6.  **Switch**: 更新 Trapframe 中的 `epc` 为 ELF 入口地址，`sp` 为新栈顶，准备返回用户态。

---

## 三、 关键实现细节与难点

### 1. VirtIO 驱动与中断机制
驱动通过 MMIO (内存映射 I/O) 与 QEMU 交互。
*   **难点**：VirtIO 协议有 Legacy (v1) 和 Modern (v2) 之分。QEMU 默认行为与驱动代码不匹配。
*   **解决**：在 Makefile 中添加 `-global virtio-mmio.force-legacy=false` 强制使用 Modern 协议，并在驱动初始化代码中正确协商 Feature 位。

### 2. 进程初始化中的“鸡蛋问题”
在 `main` 函数启动时，需要初始化文件系统 (`fsinit`)，这涉及到读取磁盘。
*   **冲突**：读取磁盘需要中断 (`virtio_disk_intr`) 来唤醒等待的进程。但此时中断可能尚未完全初始化，且 `main` 上下文中没有进程实体 (`myproc() == NULL`)。
*   **处理**：
    1.  调整 `main.c` 初始化顺序：先 `trapinithart` (开中断)，再 `binit`，最后 `fsinit`。
    2.  修改 `sleep` 函数：当检测到 `myproc() == NULL` 时（启动阶段），不进行进程调度，而是释放锁并执行 `wfi` (Wait For Interrupt) 进行自旋等待。

### 3. `exec` 的内存清理逻辑
在替换进程影像时，必须彻底清理旧的内存映射。
*   **实现**：编写 `proc_freepagetable` 函数。
*   **细节**：必须先使用 `uvmclear`（手动将 PTE 置 0）清除 `TRAMPOLINE` 和 `TRAPFRAME` 的映射，因为这两个页面是内核共享的，不能被 `kfree`。之后再调用 `freevm` 递归释放用户页表和物理页。

---

## 四、 测试与验证

本次实验编写了 `user/fstest.c` 综合测试程序，并修改了 `user/init.c` 进行自动调用。

### 1. 运行结果截图

```text
booting helloos...
kernel init done, starting processes...
init: starting fstest...
=== Starting Filesystem Tests ===
[TEST] Filesystem Integrity...
PASSED
[TEST] Concurrent Access...
PASSED
[TEST] Performance (Write 1MB)...
Time start: 1076 ms
Time end: 4031 ms
Time taken: 2955 ms
PASSED
=== All Tests Passed! ===
init: test finished.
```

### 2. 测试项分析
1.  **完整性 (Integrity)**: 成功创建文件、写入字符串、关闭、重新打开并校验内容、最后删除。证明文件系统基本逻辑闭环。
2.  **并发性 (Concurrency)**: 父子进程同时创建不同文件并写入。测试通过证明了 `bcache` 和 `inode` 锁机制有效防止了竞态条件。
3.  **性能 (Performance)**: 写入 1MB 数据（分块写入）耗时约 2.9 秒。
    *   为了测出有效时间，将时钟中断频率调整为 10ms/tick (原为 100ms)，并修复了用户态 `printf` 64位参数解析错误。
4.  **持久化 (Persistence)**: 手动测试中，写入文件后强制退出 QEMU，再次启动后文件依然存在且内容正确。

---

## 五、 遇到的问题与解决方案

本次实验调试过程极为艰难，主要解决了以下核心 Bug：

### 问题 1：启动时 Panic `holding: lk is NULL`
*   **现象**：内核启动到 `fsinit` 读取超级块时崩溃。
*   **原因**：在 `fsinit` -> `bread` -> `bget` -> `acquiresleep` 调用链中，`sleep` 函数试图获取当前进程的锁。但在 `main` 启动阶段，`myproc()` 返回 NULL，导致访问空指针偏移量（`stval=0`）。
*   **解决**：
    1.  修改 `sleeplock.c`，在获取/释放锁时增加 `if(p)` 判断。
    2.  修改 `proc.c` 的 `sleep` 函数，增加针对 `p == 0` 的特殊处理逻辑（自旋等待中断）。

### 问题 2：VirtIO 驱动卡死 (死锁)
*   **现象**：`virtio_disk_rw` 发出请求后进入 `sleep`，但永远唤醒不了，系统挂起。
*   **原因**：
    1.  **中断未开**：`main` 函数中 `fsinit` 放在了 `trapinithart` 之前，导致磁盘完成了操作发出中断，CPU 却因为 `sstatus.SIE` 关闭而忽略了中断。
    2.  **中断委托**：在 `start.c` (M-mode) 中，忘记设置 `w_mideleg(0xffff)`，导致外部中断被 M-mode 拦截，没传给 S-mode。
*   **解决**：调整初始化顺序，并在 `start.c` 正确设置中断委托。

### 问题 3：Exec 时 Panic `freewalk: leaf`
*   **现象**：`initcode` 调用 `exec` 时崩溃。
*   **原因**：`freevm` 在回收旧页表时，发现页表里还有有效的 PTE（Trampoline 页）。这是因为 `uvmunmap` (或 `uvmclear`) 只是清除了用户权限位 (`~PTE_U`)，但没有清除有效位 (`PTE_V`)，导致 `freewalk` 认为该页未释放而报警。
*   **解决**：修改 `vm.c` 中的 `uvmclear`，强制执行 `*pte = 0` 彻底清除映射。

### 问题 4：Guard Page 导致的 `uvmunmap` Panic
*   **现象**：修复上一个问题后，`exec` 报 `uvmunmap: not mapped`。
*   **原因**：我们在用户栈下方设置了 Guard Page（PTE_V=0）。当 `exec` 释放内存时，`uvmunmap` 遍历到这一页，发现它没有映射，便抛出异常。
*   **解决**：修改 `uvmunmap`，允许它跳过未映射的页面（`continue`），而不是 Panic。这是支持稀疏内存布局的必要修改。

### 问题 5：性能测试时间为 0
*   **现象**：写入 1MB 数据，`uptime` 显示耗时 0 ticks。
*   **原因**：
    1.  QEMU 时钟频率设置过低（100ms/tick），写入速度太快被忽略。
    2.  用户态 `printf` 函数在解析 64 位 `uptime` 返回值时，使用了错误的指针运算（32位步长），导致打印出垃圾值或 0。
*   **解决**：
    1.  修改 `trap.c`，提高时钟频率到 10ms。
    2.  重写 `user/ulib.c` 的 `printf`，使用 `__builtin_va_list` 正确处理 64 位参数。

### 6. 实验总结
通过本次实验，我深刻体会到了文件系统作为操作系统“持久化记忆”的重要性。从底层的寄存器位操作，到中间的锁与缓存一致性，再到上层的系统调用封装，每一个环节的疏忽（哪怕是初始化顺序颠倒、宏定义错一位）都会导致系统崩溃。

这次实验不仅让我拥有了一个可用的文件系统，更极大地锻炼了使用 `gdb`, `addr2line` 以及 `printf` 打桩法调试内核并发与内存问题的能力。